# ─── Core Deep Learning (CUDA 12.4 — RTX 3050, driver 555.99) ────────────────
# Install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# DO NOT install plain `torch` from PyPI — it will be CPU-only.
torch>=2.3.0+cu124
torchvision>=0.18.0+cu124

# ─── Dataset & Data Loading ───────────────────────────────────────────────────
fiftyone>=1.0.0
huggingface_hub>=0.20.0   # Required by fiftyone HuggingFace integration
datasets>=2.20.0          # HuggingFace datasets (optional fallback)

# ─── Computer Vision ──────────────────────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.10.0
albumentations>=1.4.0     # Data augmentation

# ─── CLIP (Multimodal Encoder) ────────────────────────────────────────────────
openai-clip>=1.0.1
ftfy>=6.1.0               # Text cleaning (required by CLIP)
regex>=2024.0.0

# ─── Scientific Computing ─────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0

# ─── Config & Experiment Management ──────────────────────────────────────────
hydra-core>=1.3.0
omegaconf>=2.3.0

# ─── Logging & Visualization ──────────────────────────────────────────────────
wandb>=0.17.0             # Experiment tracking (optional)
matplotlib>=3.9.0
seaborn>=0.13.0
tqdm>=4.66.0

# ─── Utilities ────────────────────────────────────────────────────────────────
h5py>=3.11.0
pandas>=2.2.0
