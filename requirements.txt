# ─── Core Deep Learning ───────────────────────────────────────────────────────
# torch + torchvision được cài RIÊNG với CUDA version phù hợp:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# KHÔNG liệt kê ở đây để tránh pip override mất CUDA build.

# ─── Dataset & Data Loading ───────────────────────────────────────────────────
Pillow>=10.0.0
svgpathtools>=1.6.0       # SVG path parsing for build_dataset.py
gdown>=5.1.0              # Google Drive download

# ─── Scientific Computing ─────────────────────────────────────────────────────
numpy>=1.26.0

# ─── Logging & Visualization ──────────────────────────────────────────────────
matplotlib>=3.9.0
tqdm>=4.66.0
